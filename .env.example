# --- PRODUCTION CONFIGURATION EXAMPLE ---

# Laravel Application
APP_NAME=Insamo
APP_ENV=production
APP_DEBUG=true
APP_KEY=
APP_URL=https://apiapp.insamo.id
FRONTEND_URL=https://app.insamo.id

# Database Settings
# Gunakan user khusus untuk aplikasi, root tetap private
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=insamo_rw
DB_USERNAME=insamo
DB_PASSWORD=your_app_db_password
DB_PORT_EXTERNAL=3306

# MySQL Root (Hanya untuk akses internal docker)
MYSQL_ROOT_PASSWORD=your_root_db_password

# JWT Settings
JWT_SECRET=
JWT_ALGO=HS256

# Frontend Settings
VITE_API_URL=https://apiapp.insamo.id/api
